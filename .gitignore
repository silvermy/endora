# Local config — contains RTSP credentials, never commit
endora/config/settings.yaml

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Models — hosted as GitHub Release assets, downloaded at Docker build time
*.onnx
*.pt

# Data
endora/data/
*.jsonl

# Stray device dumps and scratch files at the repo root — these have landed
# in commits before via "git add -A" (two NanoStation JSON dumps from an
# unrelated network job, in v1.9.177).
/*.json
.DS_Store
